from django.urls import path, include
from watchlist_app.views import movielist, movie_details

urlpatterns = [
    path('list/', movielist, name="Movie_List"),
    path('<int:reqpk>/', movie_details, name="movie_details"),

]
